---
import Layout from '../layouts/Layout.astro';
import { courses } from '../lib/courses';
import { generateCoursesListSchema } from '../lib/schemas';

const courseSchema = generateCoursesListSchema(courses);
---

<Layout>
  <div>
    <!-- Hero Section -->
    <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-16 md:py-24 lg:py-32">
      <div class="max-w-6xl mx-auto px-4">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-4">
          Nossos Cursos
        </h1>
        <p class="text-lg md:text-xl text-blue-100 max-w-2xl">
          Especializações profissionais em segurança, eficiência e excelência no transporte
        </p>
      </div>
    </section>

    <!-- Courses Grid -->
    <section class="py-16 md:py-20 lg:py-24 bg-gray-50">
      <div class="max-w-6xl mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {courses.map((course) => (
            <div 
              class="bg-white rounded-md shadow-md hover:shadow-lg transition-all duration-300 overflow-hidden hover:scale-105 h-full flex flex-col"
            >
              <!-- Course Image -->
              <div class="relative h-48 md:h-56 bg-gray-200 overflow-hidden">
                <img 
                  src={course.img} 
                  alt={course.nome}
                  class="w-full h-full object-cover"
                />
                <div class={`absolute top-4 right-4 p-3 rounded-md text-white ${
                  course.cor === 'primary' ? 'bg-blue-600' : 'bg-amber-500'
                }`}>
                  <span class="material-symbols-outlined">
                    {course.icone}
                  </span>
                </div>
              </div>
              
              <!-- Course Content -->
              <div class="p-6 flex-1 flex flex-col">
                <h3 class="text-xl md:text-2xl font-bold text-gray-900 mb-2">
                  {course.nome}
                </h3>
                <p class="text-sm text-gray-600 mb-4">
                  {course.titulo}
                </p>
                <p class="text-gray-700 text-sm mb-4 flex-1">
                  {course.descricao}
                </p>
                
                <!-- Benefits -->
                <div class="mb-4 flex-1">
                  {course.beneficios.map((beneficio) => (
                    <div class="flex items-start gap-2 mb-2">
                      <span class="material-symbols-outlined text-sm text-green-600 flex-shrink-0 mt-1">
                        done
                      </span>
                      <span class="text-sm text-gray-700">{beneficio}</span>
                    </div>
                  ))}
                </div>

                <!-- Pricing -->
                <div class="bg-gray-50 p-4 rounded-md mb-6">
                  <p class="text-xs text-gray-500 line-through mb-1">
                    De {course.precoOriginal}
                  </p>
                  <p class={`text-2xl font-bold mb-1 ${
                    course.cor === 'primary' ? 'text-blue-600' : 'text-amber-600'
                  }`}>
                    {course.preco}
                  </p>
                  <p class="text-xs text-gray-600">
                    {course.parcelado}
                  </p>
                </div>

                <!-- CTA Buttons -->
                <div class="flex gap-2">
                  <a 
                    href={course.link}
                    class={`flex-1 text-center py-3 rounded-md font-semibold transition-colors duration-300 ${
                      course.cor === 'primary' 
                        ? 'bg-blue-600 hover:bg-blue-700 text-white' 
                        : 'bg-amber-500 hover:bg-amber-600 text-white'
                    }`}
                  >
                    Ver Detalhes
                  </a>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Doubt CTA Section -->
    <section class="bg-white py-16 md:py-20 lg:py-24 border-t-4 border-blue-600">
      <div class="max-w-4xl mx-auto px-4 text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          Não Sabe Qual Curso Escolher?
        </h2>
        <p class="text-lg text-gray-700 mb-8">
          Fale conosco! Nossos especialistas estão prontos para ajudá-lo a escolher o curso ideal para sua carreira.
        </p>
        <a
          href="https://wa.me/18999999999?text=Olá! Gostaria de saber mais sobre os cursos da Autoescola Ideal."
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 bg-green-500 hover:bg-green-600 text-white px-8 py-4 rounded-md font-semibold transition-colors duration-300"
        >
          <span class="material-symbols-outlined">chat</span>
          Fale Conosco no WhatsApp
        </a>
      </div>
    </section>
  </div>

  <!-- Schema Markup for SEO -->
  <script type="application/ld+json" set:html={JSON.stringify(courseSchema)} />
</Layout>
